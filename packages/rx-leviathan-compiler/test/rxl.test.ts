import { compile } from '../lib/rxlc';

describe('RxLc Compiler', () => {
    describe('compile()', () => {
        it('needs tests', () => {
            compile([global['__RXL_PATH__']]);
        });
    });
});
